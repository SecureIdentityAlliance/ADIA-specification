name: Publish

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-18.04

    steps:
      - name: Install bikeshed
        run: sudo apt-get update -y & sudo apt-get -y install python3-pip & pip3 install bikeshed && bikeshed update

      - uses: actions/checkout@v2

      - name: Output to HTML
        run: ./compile.sh

      - name: Publish HTML to GitHub Pages
        if: success()
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
